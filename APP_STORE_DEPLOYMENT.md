# 🚀 Руководство по деплою в App Store

## 📋 Чеклист готовности

### ✅ Что уже готово:
- [x] Подписка разработчика Apple
- [x] RevenueCat настроен (test ключ)
- [x] Мобильное приложение с подписками
- [x] Backend API (FastAPI)
- [x] Onboarding экраны
- [x] Функционал расчета процента жира

### ⚠️ Что нужно сделать:

## 1. 🌐 Деплой Backend сервера

### Варианты хостинга (рекомендуемые):

#### **Вариант 1: Railway.app** (самый простой, ~$5-20/мес)
- ✅ Автоматический деплой из GitHub
- ✅ Бесплатный SSL сертификат
- ✅ Простая настройка
- ✅ Автоматическое масштабирование

**Инструкция:**
1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект
3. Подключите GitHub репозиторий
4. Выберите папку `backend`
5. Railway автоматически определит Python
6. Добавьте переменные окружения:
   - `OPENAI_API_KEY=ваш_ключ`
   - `OPENAI_MODEL=gpt-4o-mini`
7. Railway даст вам URL типа: `https://your-app.railway.app`
8. Скопируйте этот URL - он понадобится для мобильного приложения

#### **Вариант 2: Render.com** (бесплатный тариф доступен)
- ✅ Бесплатный тариф для начала
- ✅ Автоматический деплой
- ✅ SSL включен

**Инструкция:**
1. Зарегистрируйтесь на https://render.com
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Настройки:
   - **Root Directory**: `backend`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`
5. Добавьте переменные окружения
6. Получите URL: `https://your-app.onrender.com`

#### **Вариант 3: DigitalOcean App Platform** (~$5-12/мес)
- ✅ Надежный хостинг
- ✅ Хорошая производительность

#### **Вариант 4: AWS/GCP/Azure** (для масштабирования)
- ✅ Максимальная гибкость
- ⚠️ Требует больше настройки

### Настройка CORS для production

После деплоя backend, обновите `backend/main.py`:

```python
# Замените строку 16:
allow_origins=["*"],  # В продакшене указать конкретные домены

# На:
allow_origins=[
    "https://your-backend-domain.com",
    "capacitor://localhost",
    "ionic://localhost",
    "http://localhost",
    "http://localhost:8080",
    "http://localhost:8100"
],
```

## 2. 📱 Настройка Production API URL в мобильном приложении

Откройте `mobile/src/api/client.ts` и обновите функцию `getApiBaseUrl`:

```typescript
const getApiBaseUrl = (): string => {
  if (__DEV__) {
    // Режим разработки - используем локальный IP
    if (Platform.OS === 'android') {
      return 'http://10.0.2.2:8000';
    } else {
      return `http://${COMPUTER_IP}:8000`;
    }
  }
  // PRODUCTION: Замените на ваш реальный URL backend
  return 'https://your-backend-domain.com';  // <-- ВАШ URL СЕРВЕРА
};
```

**Важно:** Используйте `https://`, не `http://` для production!

## 3. 🔐 Настройка RevenueCat для Production

### 3.1 Получите Production API ключ

1. Войдите в RevenueCat Dashboard: https://app.revenuecat.com
2. Выберите ваш проект
3. Перейдите в **Project Settings** → **API Keys**
4. Скопируйте **Public SDK Key** (не Secret Key!)
5. Обновите `mobile/src/services/subscription.ts`:

```typescript
// Замените test ключ на production:
const REVENUECAT_API_KEY_IOS = 'appl_YOUR_PRODUCTION_KEY_HERE';
```

### 3.2 Настройте App Store Connect

1. Войдите в **App Store Connect**: https://appstoreconnect.apple.com
2. Создайте новое приложение:
   - **Bundle ID**: `com.bodyfatai.app` (должен совпадать с `app.json`)
   - **Name**: BodyFatAI (или ваше название)
   - **Primary Language**: English
   - **SKU**: bodyfatai-001

3. Создайте подписки (см. `NEXT_STEPS.md`):
   - Subscription Group: "Premium"
   - Monthly: `monthly_premium` (или `monthly`)
   - Annual: `yearly_premium` (или `yearly`)

4. Подключите App Store Connect к RevenueCat:
   - В RevenueCat: **Integrations** → **App Store Connect**
   - Нажмите **Connect**
   - Следуйте инструкциям (может потребоваться API ключ App Store Connect)

### 3.3 Создайте Entitlement

1. В RevenueCat: **Entitlements** → **+ New Entitlement**
2. Identifier: `premium`
3. Привяжите продукты: `monthly_premium`, `yearly_premium`

## 4. 🏗️ Сборка iOS приложения

### 4.1 Установите EAS CLI (если еще не установлен)

```bash
npm install -g eas-cli
```

### 4.2 Войдите в Expo аккаунт

```bash
cd "Body Fat/mobile"
eas login
```

### 4.3 Настройте EAS Build

```bash
eas build:configure
```

Это создаст файл `eas.json` с конфигурацией сборки.

### 4.4 Обновите `app.json` для production

Убедитесь, что в `app.json` указаны правильные данные:

```json
{
  "expo": {
    "name": "BodyFatAI",
    "slug": "bodyfatai",
    "version": "1.0.0",
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.bodyfatai.app",
      "buildNumber": "1"
    }
  }
}
```

### 4.5 Создайте production сборку

```bash
eas build --platform ios --profile production
```

**Важно:** Для первой сборки вам понадобится:
- Apple Developer аккаунт (у вас уже есть)
- Сертификаты будут созданы автоматически EAS

### 4.6 Альтернатива: Сборка через Xcode

Если хотите собрать локально:

```bash
# Создайте development build
eas build --platform ios --profile development --local

# Или используйте Expo для генерации нативного проекта
npx expo prebuild
```

Затем откройте `ios/BodyFatAI.xcworkspace` в Xcode и соберите оттуда.

## 5. 📤 Загрузка в App Store Connect

### 5.1 Через EAS Submit (рекомендуется)

```bash
eas submit --platform ios
```

EAS автоматически загрузит сборку в App Store Connect.

### 5.2 Через Xcode

1. Откройте Xcode
2. **Product** → **Archive**
3. После архивации: **Distribute App**
4. Выберите **App Store Connect**
5. Следуйте инструкциям

### 5.3 Через Transporter (альтернатива)

1. Скачайте Transporter из Mac App Store
2. Экспортируйте `.ipa` файл из Xcode
3. Загрузите через Transporter

## 6. ✅ Финальная проверка перед релизом

### Backend:
- [ ] Backend развернут и доступен по HTTPS
- [ ] API URL обновлен в `client.ts`
- [ ] CORS настроен правильно
- [ ] OpenAI API ключ добавлен в переменные окружения
- [ ] Протестированы все endpoints

### Mobile App:
- [ ] Production RevenueCat ключ установлен
- [ ] API URL указывает на production сервер
- [ ] Bundle ID совпадает с App Store Connect
- [ ] Версия приложения обновлена
- [ ] Иконка и splash screen готовы
- [ ] Onboarding работает
- [ ] Подписки работают (протестировано в Sandbox)

### App Store Connect:
- [ ] Приложение создано
- [ ] Подписки созданы и активны
- [ ] App Store Connect подключен к RevenueCat
- [ ] Entitlement `premium` создан
- [ ] Тестовые аккаунты Sandbox созданы

### Тестирование:
- [ ] Протестирована покупка подписки (Sandbox)
- [ ] Протестирован расчет процента жира
- [ ] Протестирован анализ фото
- [ ] Протестированы советы
- [ ] Протестирован onboarding
- [ ] Протестирована работа без подписки (free trial)

## 7. 📝 Заполнение информации в App Store Connect

1. **App Information**:
   - Название, подзаголовок, описание
   - Ключевые слова
   - Категория: Health & Fitness
   - Возрастной рейтинг

2. **Pricing and Availability**:
   - Цена приложения (обычно Free)
   - Доступность по странам

3. **App Privacy**:
   - Укажите, что собираете данные о здоровье
   - Укажите использование фото (для анализа)

4. **Screenshots**:
   - Требуются скриншоты для разных размеров iPhone
   - Минимум: iPhone 6.7" (iPhone 14 Pro Max)

5. **App Review Information**:
   - Контактная информация
   - Примечания для ревьюеров (если нужно)

## 8. 🎯 Отправка на ревью

1. В App Store Connect: **TestFlight** → добавьте внутренних тестеров
2. Протестируйте через TestFlight
3. Если все работает: **App Store** → **+ Version**
4. Заполните всю информацию
5. Нажмите **Submit for Review**

## 9. 💰 Рекомендуемые цены на подписки

- **Monthly**: $4.99 - $9.99
- **Annual**: $39.99 - $79.99 (скидка ~30-40%)

## 10. 🔧 Полезные команды

```bash
# Проверка статуса сборки
eas build:list

# Просмотр логов сборки
eas build:view [BUILD_ID]

# Обновление приложения (после изменений)
eas build --platform ios --profile production

# Проверка конфигурации
eas build:configure
```

## ❓ Частые проблемы

### Backend не отвечает
- Проверьте, что сервер запущен
- Проверьте CORS настройки
- Проверьте логи на хостинге

### Подписки не работают
- Убедитесь, что используете production ключ RevenueCat
- Проверьте, что App Store Connect подключен
- Проверьте, что Entitlement создан

### Ошибка при сборке
- Проверьте Bundle ID
- Убедитесь, что Apple Developer аккаунт активен
- Проверьте сертификаты в EAS

## 📞 Поддержка

- **Expo Docs**: https://docs.expo.dev
- **EAS Build**: https://docs.expo.dev/build/introduction/
- **RevenueCat Docs**: https://docs.revenuecat.com
- **App Store Connect Help**: https://help.apple.com/app-store-connect/

---

**Удачи с релизом! 🚀**

