# 🚀 Деплой Backend на Railway.app (ПЛАТНО, но надежнее)

## 💰 Цены:
- **Hobby Plan**: $5/мес (500 часов, $0.01 за дополнительный час)
- **Pro Plan**: $20/мес (неограниченно)

## 📋 Шаг 1: Регистрация на Railway

1. Зайдите на https://railway.app
2. Нажмите **"Start a New Project"**
3. Зарегистрируйтесь через GitHub (рекомендуется)
4. Подтвердите доступ к GitHub репозиторию

## 📋 Шаг 2: Подготовка репозитория GitHub

**ВАЖНО:** Railway работает только с GitHub репозиторием!

Если у вас еще нет репозитория, создайте его (см. инструкцию в `DEPLOY_RENDER.md`, шаг 2).

## 📋 Шаг 3: Создание проекта на Railway

1. В Railway Dashboard нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий
4. Railway автоматически определит, что это Python проект

## 📋 Шаг 4: Настройка сервиса

### 4.1 Выбор папки

1. Railway может автоматически определить папку `backend`
2. Если нет - нажмите на сервис → **Settings** → **Root Directory**
3. Установите: `backend`

### 4.2 Переменные окружения

1. В настройках сервиса найдите **"Variables"**
2. Нажмите **"New Variable"**
3. Добавьте:

   **OPENAI_API_KEY**
   - Key: `OPENAI_API_KEY`
   - Value: ваш OpenAI API ключ

   **OPENAI_MODEL**
   - Key: `OPENAI_MODEL`
   - Value: `gpt-4o-mini`

### 4.3 Настройка команды запуска

Railway обычно автоматически определяет команду, но можно проверить:

1. **Settings** → **Deploy**
2. **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### 4.4 План

1. **Settings** → **Usage**
2. Выберите **Hobby Plan** ($5/мес) или оставьте Pay-as-you-go

## 📋 Шаг 5: Получение URL

1. После деплоя Railway автоматически создаст URL
2. Нажмите на сервис → **Settings** → **Domains**
3. Вы увидите URL типа: `https://your-app.up.railway.app`
4. **Скопируйте этот URL!**

### Настройка кастомного домена (опционально):

1. В **Settings** → **Domains** нажмите **"Generate Domain"**
2. Или добавьте свой домен

## 📋 Шаг 6: Проверка работы

1. Откройте ваш URL: `https://your-app.up.railway.app`
2. Проверьте API: `https://your-app.up.railway.app/docs`
3. Должна открыться Swagger документация

## ✅ Преимущества Railway:

- ✅ Сервис не "засыпает" (в отличие от Render free)
- ✅ Быстрее работает
- ✅ Проще настройка
- ✅ Автоматический SSL
- ✅ Хорошие логи

## 🔧 Обновление кода

1. Закоммитьте изменения:
```bash
git add .
git commit -m "Update backend"
git push
```

2. Railway автоматически задеплоит новую версию

## 📝 Следующий шаг

После успешного деплоя:
1. Обновите CORS в `backend/main.py`
2. Обновите API URL в `mobile/src/api/client.ts`

---

**Готово! Ваш backend теперь работает на Railway! 🎉**

