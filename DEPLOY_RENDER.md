# 🚀 Деплой Backend на Render.com (БЕСПЛАТНО)

## 📋 Шаг 1: Регистрация на Render

1. Зайдите на https://render.com
2. Нажмите **"Get Started for Free"**
3. Зарегистрируйтесь через GitHub (рекомендуется) или email
4. Подтвердите email (если регистрация через email)

## 📋 Шаг 2: Подготовка репозитория GitHub

**ВАЖНО:** Render работает только с GitHub репозиторием!

### Если у вас еще нет репозитория:

1. Создайте репозиторий на GitHub:
   - Зайдите на https://github.com/new
   - Название: `bodyfatai-backend` (или любое другое)
   - Сделайте его **Private** (если не хотите публичный код)
   - Нажмите **Create repository**

2. Загрузите код в репозиторий:

```bash
# Откройте PowerShell в папке проекта
cd "C:\Users\bumer\OneDrive\Desktop\code\Body Fat"

# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit"

# Подключите к GitHub (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Загрузите код
git branch -M main
git push -u origin main
```

**Если у вас уже есть репозиторий:**
- Просто убедитесь, что код загружен в GitHub

## 📋 Шаг 3: Создание Web Service на Render

1. Войдите в Render Dashboard: https://dashboard.render.com
2. Нажмите **"New +"** → **"Web Service"**
3. Выберите **"Connect account"** (если еще не подключен GitHub)
4. Выберите ваш репозиторий с backend кодом
5. Нажмите **"Connect"**

## 📋 Шаг 4: Настройка Web Service

Заполните форму:

### Основные настройки:
- **Name**: `bodyfatai-backend` (или любое название)
- **Region**: Выберите ближайший (например, `Frankfurt` для Европы)
- **Branch**: `main` (или ваша основная ветка)
- **Root Directory**: `backend` ⚠️ **ВАЖНО!**
- **Runtime**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### План:
- Выберите **"Free"** (бесплатный план)

### Переменные окружения (Environment Variables):
Нажмите **"Add Environment Variable"** и добавьте:

1. **OPENAI_API_KEY**
   - Key: `OPENAI_API_KEY`
   - Value: ваш OpenAI API ключ (начинается с `sk-...`)

2. **OPENAI_MODEL**
   - Key: `OPENAI_MODEL`
   - Value: `gpt-4o-mini`

### Дополнительные настройки:
- **Auto-Deploy**: `Yes` (автоматический деплой при push в GitHub)

## 📋 Шаг 5: Создание сервиса

1. Нажмите **"Create Web Service"**
2. Render начнет деплой (это займет 3-5 минут)
3. Вы увидите логи сборки в реальном времени

## 📋 Шаг 6: Получение URL

После успешного деплоя:

1. В верхней части страницы сервиса вы увидите URL
2. Он будет выглядеть так: `https://bodyfatai-backend.onrender.com`
3. **Скопируйте этот URL** - он понадобится для мобильного приложения!

## 📋 Шаг 7: Проверка работы

1. Откройте ваш URL в браузере: `https://your-app.onrender.com`
2. Должна открыться страница с сообщением или веб-интерфейс
3. Проверьте API документацию: `https://your-app.onrender.com/docs`
4. Должна открыться Swagger документация FastAPI

## ⚠️ Важные замечания

### Бесплатный план Render:
- ✅ Бесплатный SSL (HTTPS)
- ✅ Автоматический деплой
- ⚠️ Сервис "засыпает" после 15 минут неактивности
- ⚠️ Первый запрос после "сна" может занять 30-60 секунд (пробуждение)
- ⚠️ Ограничение: 750 часов в месяц (достаточно для тестирования)

### Если нужна постоянная работа:
- Перейдите на **Starter Plan** ($7/мес) - сервис не будет "засыпать"
- Или используйте Railway ($5-20/мес)

## 🔧 Обновление кода

После изменений в коде:

1. Закоммитьте изменения:
```bash
git add .
git commit -m "Update backend"
git push
```

2. Render автоматически задеплоит новую версию (если включен Auto-Deploy)

## 🐛 Решение проблем

### Ошибка при деплое:
- Проверьте логи в Render Dashboard
- Убедитесь, что `Root Directory` указан как `backend`
- Проверьте, что `requirements.txt` существует

### Сервис не отвечает:
- Подождите 30-60 секунд (может "просыпаться")
- Проверьте логи в Render Dashboard
- Убедитесь, что переменные окружения установлены

### CORS ошибки:
- Обновите `backend/main.py` (см. следующий шаг)

## 📝 Следующий шаг

После успешного деплоя:
1. Обновите CORS в `backend/main.py` (добавьте ваш Render URL)
2. Обновите API URL в `mobile/src/api/client.ts`

---

**Готово! Ваш backend теперь работает на Render! 🎉**

