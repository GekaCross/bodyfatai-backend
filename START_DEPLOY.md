# 🚀 Начните деплой Backend - Выберите вариант

## 🎯 Рекомендация: Начните с **Render** (БЕСПЛАТНО)

### Почему Render:
- ✅ **БЕСПЛАТНО** для начала
- ✅ Простая настройка
- ✅ Автоматический SSL
- ⚠️ Может "засыпать" после 15 мин неактивности (но это нормально для тестирования)

### Когда перейти на Railway:
- Когда приложение начнет приносить доход
- Когда нужна постоянная работа без "сна"
- Когда нужна большая производительность

---

## 📋 Быстрый старт с Render (5 минут)

### 1. Убедитесь, что код в GitHub

Если еще нет репозитория:

```bash
# В PowerShell в папке проекта
cd "C:\Users\bumer\OneDrive\Desktop\code\Body Fat"

# Инициализируйте git
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub (https://github.com/new)
# Затем подключите:
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

### 2. Зайдите на Render

1. Откройте https://render.com
2. Зарегистрируйтесь через GitHub
3. Нажмите **"New +"** → **"Web Service"**
4. Выберите ваш репозиторий

### 3. Настройте сервис

**Важные настройки:**
- **Root Directory**: `backend` ⚠️
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`
- **Plan**: Free

**Переменные окружения:**
- `OPENAI_API_KEY` = ваш ключ OpenAI
- `OPENAI_MODEL` = `gpt-4o-mini`

### 4. Получите URL

После деплоя скопируйте URL (например: `https://bodyfatai-backend.onrender.com`)

### 5. Проверьте

Откройте `https://your-url.onrender.com/docs` - должна открыться документация API

---

## 📋 Или используйте Railway ($5/мес)

Если хотите более надежный вариант:

1. Откройте https://railway.app
2. **New Project** → **Deploy from GitHub repo**
3. Выберите репозиторий
4. Добавьте переменные окружения
5. Получите URL

**Подробная инструкция:** см. `DEPLOY_RAILWAY.md`

---

## ✅ После деплоя

1. **Скопируйте URL** вашего backend (например: `https://bodyfatai-backend.onrender.com`)
2. **Обновите** `mobile/src/api/client.ts` (строка 26):
   ```typescript
   return 'https://ваш-url.onrender.com';  // <-- Вставьте ваш URL
   ```
3. **Обновите CORS** в `backend/main.py` (добавьте ваш URL в список разрешенных)

---

## 📚 Подробные инструкции

- **Render (бесплатно)**: `DEPLOY_RENDER.md`
- **Railway (платно)**: `DEPLOY_RAILWAY.md`

---

**Начните с Render - это бесплатно и достаточно для старта! 🚀**

