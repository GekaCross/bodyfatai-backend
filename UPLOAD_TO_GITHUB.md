# 📤 Загрузка кода на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Заполните форму:
   - **Repository name**: `bodyfatai-backend` (или любое название)
   - **Visibility**: Public или Private (на ваше усмотрение)
   - **НЕ включайте** README, .gitignore, license (у нас уже есть код)
2. Нажмите **"Create repository"**

## Шаг 2: Загрузите код через PowerShell

Откройте PowerShell в папке проекта и выполните команды:

```powershell
# Перейдите в папку проекта
cd "C:\Users\bumer\OneDrive\Desktop\code\Body Fat"

# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: BodyFatAI backend"

# Подключите к GitHub (замените YOUR_USERNAME и REPO_NAME на ваши данные)
# Например: git remote add origin https://github.com/GekaCross/bodyfatai-backend.git
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Загрузите код
git branch -M main
git push -u origin main
```

## ⚠️ Важно:

1. **Замените `YOUR_USERNAME`** на ваш GitHub username (например, `GekaCross`)
2. **Замените `REPO_NAME`** на название репозитория, которое вы указали при создании

## 🔐 Если попросит авторизацию:

GitHub может попросить ввести логин и пароль. Используйте:
- **Username**: ваш GitHub username
- **Password**: Personal Access Token (не обычный пароль!)

### Как создать Personal Access Token:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите scope: `repo` (полный доступ к репозиториям)
4. Скопируйте токен и используйте его как пароль

## ✅ После успешной загрузки:

Вернитесь на Render и выберите ваш новый репозиторий!

