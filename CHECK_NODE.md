# Проверка установки Node.js

После установки Node.js выполните в терминале:

```powershell
node --version
npm --version
```

Должны увидеть что-то вроде:
```
v20.10.0
10.2.3
```

Если команды не работают, перезапустите терминал или VS Code.


